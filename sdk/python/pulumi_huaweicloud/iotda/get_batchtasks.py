# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities
from . import outputs

__all__ = [
    'GetBatchtasksResult',
    'AwaitableGetBatchtasksResult',
    'get_batchtasks',
    'get_batchtasks_output',
]

@pulumi.output_type
class GetBatchtasksResult:
    """
    A collection of values returned by getBatchtasks.
    """
    def __init__(__self__, batchtasks=None, id=None, region=None, space_id=None, status=None, type=None):
        if batchtasks and not isinstance(batchtasks, list):
            raise TypeError("Expected argument 'batchtasks' to be a list")
        pulumi.set(__self__, "batchtasks", batchtasks)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if region and not isinstance(region, str):
            raise TypeError("Expected argument 'region' to be a str")
        pulumi.set(__self__, "region", region)
        if space_id and not isinstance(space_id, str):
            raise TypeError("Expected argument 'space_id' to be a str")
        pulumi.set(__self__, "space_id", space_id)
        if status and not isinstance(status, str):
            raise TypeError("Expected argument 'status' to be a str")
        pulumi.set(__self__, "status", status)
        if type and not isinstance(type, str):
            raise TypeError("Expected argument 'type' to be a str")
        pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter
    def batchtasks(self) -> Sequence['outputs.GetBatchtasksBatchtaskResult']:
        """
        All batch tasks that match the filter parameters.
        The batchtasks structure is documented below.
        """
        return pulumi.get(self, "batchtasks")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The provider-assigned unique ID for this managed resource.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def region(self) -> _builtins.str:
        return pulumi.get(self, "region")

    @_builtins.property
    @pulumi.getter(name="spaceId")
    def space_id(self) -> Optional[_builtins.str]:
        return pulumi.get(self, "space_id")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        The status of the batch task.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> _builtins.str:
        """
        The batch task type.
        """
        return pulumi.get(self, "type")


class AwaitableGetBatchtasksResult(GetBatchtasksResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetBatchtasksResult(
            batchtasks=self.batchtasks,
            id=self.id,
            region=self.region,
            space_id=self.space_id,
            status=self.status,
            type=self.type)


def get_batchtasks(region: Optional[_builtins.str] = None,
                   space_id: Optional[_builtins.str] = None,
                   status: Optional[_builtins.str] = None,
                   type: Optional[_builtins.str] = None,
                   opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetBatchtasksResult:
    """
    Use this data source to get the list of IoTDA batch tasks within HuaweiCloud.

    > When accessing an IoTDA **standard** or **enterprise** edition instance, you need to specify the IoTDA service
      endpoint in `provider` block.
      You can login to the IoTDA console, choose the instance **Overview** and click **Access Details**
      to view the HTTPS application access address. An example of the access address might be
      **9bc34xxxxx.st1.iotda-app.ap-southeast-1.myhuaweicloud.com**, then you need to configure the
      `provider` block as follows:

    ```python
    import pulumi
    ```

    ## Example Usage

    ```python
    import pulumi
    import pulumi_huaweicloud as huaweicloud

    config = pulumi.Config()
    type = config.require_object("type")
    test = huaweicloud.IoTDA.get_batchtasks(type=type)
    ```


    :param _builtins.str region: Specifies the region in which to query the batch tasks.
           If omitted, the provider-level region will be used.
    :param _builtins.str space_id: Specifies the space ID.
           If omitted, query all batch tasks under the current instance.
    :param _builtins.str status: Specifies the status of the batch task.
           The valid values are as follows:
           + **Initializing**
           + **Waitting**
           + **Processing**
           + **Success**
           + **Fail**
           + **PartialSuccess**
           + **Stopped**
           + **Stopping**
    :param _builtins.str type: Specifies the type of the batch task.
           The valid values are as follows:
           + **createDevices**: Batch create devices task.
           + **updateDevices**: Batch update devices task.
           + **deleteDevices**: Batch deletion of devices task.
           + **freezeDevices**: Batch freeze devices task.
           + **unfreezeDevices**: Batch unfreeze devices task.
    """
    __args__ = dict()
    __args__['region'] = region
    __args__['spaceId'] = space_id
    __args__['status'] = status
    __args__['type'] = type
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('huaweicloud:IoTDA/getBatchtasks:getBatchtasks', __args__, opts=opts, typ=GetBatchtasksResult).value

    return AwaitableGetBatchtasksResult(
        batchtasks=pulumi.get(__ret__, 'batchtasks'),
        id=pulumi.get(__ret__, 'id'),
        region=pulumi.get(__ret__, 'region'),
        space_id=pulumi.get(__ret__, 'space_id'),
        status=pulumi.get(__ret__, 'status'),
        type=pulumi.get(__ret__, 'type'))
def get_batchtasks_output(region: Optional[pulumi.Input[Optional[_builtins.str]]] = None,
                          space_id: Optional[pulumi.Input[Optional[_builtins.str]]] = None,
                          status: Optional[pulumi.Input[Optional[_builtins.str]]] = None,
                          type: Optional[pulumi.Input[_builtins.str]] = None,
                          opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetBatchtasksResult]:
    """
    Use this data source to get the list of IoTDA batch tasks within HuaweiCloud.

    > When accessing an IoTDA **standard** or **enterprise** edition instance, you need to specify the IoTDA service
      endpoint in `provider` block.
      You can login to the IoTDA console, choose the instance **Overview** and click **Access Details**
      to view the HTTPS application access address. An example of the access address might be
      **9bc34xxxxx.st1.iotda-app.ap-southeast-1.myhuaweicloud.com**, then you need to configure the
      `provider` block as follows:

    ```python
    import pulumi
    ```

    ## Example Usage

    ```python
    import pulumi
    import pulumi_huaweicloud as huaweicloud

    config = pulumi.Config()
    type = config.require_object("type")
    test = huaweicloud.IoTDA.get_batchtasks(type=type)
    ```


    :param _builtins.str region: Specifies the region in which to query the batch tasks.
           If omitted, the provider-level region will be used.
    :param _builtins.str space_id: Specifies the space ID.
           If omitted, query all batch tasks under the current instance.
    :param _builtins.str status: Specifies the status of the batch task.
           The valid values are as follows:
           + **Initializing**
           + **Waitting**
           + **Processing**
           + **Success**
           + **Fail**
           + **PartialSuccess**
           + **Stopped**
           + **Stopping**
    :param _builtins.str type: Specifies the type of the batch task.
           The valid values are as follows:
           + **createDevices**: Batch create devices task.
           + **updateDevices**: Batch update devices task.
           + **deleteDevices**: Batch deletion of devices task.
           + **freezeDevices**: Batch freeze devices task.
           + **unfreezeDevices**: Batch unfreeze devices task.
    """
    __args__ = dict()
    __args__['region'] = region
    __args__['spaceId'] = space_id
    __args__['status'] = status
    __args__['type'] = type
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('huaweicloud:IoTDA/getBatchtasks:getBatchtasks', __args__, opts=opts, typ=GetBatchtasksResult)
    return __ret__.apply(lambda __response__: GetBatchtasksResult(
        batchtasks=pulumi.get(__response__, 'batchtasks'),
        id=pulumi.get(__response__, 'id'),
        region=pulumi.get(__response__, 'region'),
        space_id=pulumi.get(__response__, 'space_id'),
        status=pulumi.get(__response__, 'status'),
        type=pulumi.get(__response__, 'type')))
